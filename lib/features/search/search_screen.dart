import 'package:flutter/material.dart';

class SearchScnree extends StatelessWidget {
  const SearchScnree({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}